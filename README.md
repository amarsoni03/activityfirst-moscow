# ActivityFirst Moscow

Find activities that fit your free time — metro-first, activity-first, not a studio directory.

```bash
npm install
npm run dev
```

Opens on port 3000. Hash routes: `#explore`, `#activity/{id}`, `#my-week`, `#free-time`, `#dashboard`, `#provider`, `#post`.
